<%# app/views/bookmarks/new.html.erb %>
<div class="container mt-4">
  <h1>Add a movie to <strong><%= @list.name %></strong></h1>

  <%= render "form" %>

  <%= link_to "Back to list", list_path(@list), class: "btn btn-secondary mt-3" %>
</div>
